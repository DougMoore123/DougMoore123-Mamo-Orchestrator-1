[project]
name = "mamo-orchestrator"
version = "0.1.0"
description = "Manufacturing orchestration demo with CR scheduling and RAG"
readme = "README.md"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
